export default function LoginPage() {
  return (
    <section>
      <h1>Auth API</h1>
      <p>Use POST /api/auth/register and /api/auth/login for onboarding. New users require admin approval.</p>
    </section>
  );
}
